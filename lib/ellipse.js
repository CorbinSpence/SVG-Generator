class Ellipse extends Shape{
    constructor(color, xRadius, yRadius, centerPos){
        super(color)
        this.xRadius = xRadius
        this.yRadius = yRadius
        this.centerPos = centerPos
    }
}